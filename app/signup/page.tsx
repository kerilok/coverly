"use client";
import Link from"next/link";import{useState}from"react";import{useRouter}from"next/navigation";import{BookOpen,ArrowRight}from"lucide-react";import{toast}from"sonner";import{createClient}from"@/lib/supabase/client";
export default function Signup(){const router=useRouter(),supabase=createClient();const[loading,setLoading]=useState(false);const[form,setForm]=useState({username:"",email:"",password:""});
 async function submit(e:React.FormEvent){e.preventDefault();if(!supabase)return toast.error("Сначала добавьте ключи Supabase в Vercel");if(form.username.trim().length<2)return toast.error("Имя должно содержать минимум 2 символа");if(form.password.length<6)return toast.error("Пароль должен содержать минимум 6 символов");setLoading(true);const{data,error}=await supabase.auth.signUp({email:form.email,password:form.password,options:{data:{username:form.username.trim()},emailRedirectTo:`${location.origin}/auth/callback`}});setLoading(false);if(error)return toast.error(error.message);if(data.session){toast.success("Аккаунт создан");router.push("/account")}else toast.success("Проверьте почту и подтвердите регистрацию")}
 return <main className="grid min-h-screen place-items-center p-4"><div className="card w-full max-w-md rounded-3xl p-8 shadow-soft"><Link href="/" className="flex items-center gap-2 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-acid text-ink"><BookOpen size={18}/></span>coverly</Link><h1 className="mt-9 text-3xl font-bold">Создать аккаунт</h1><p className="muted mt-2">Ваше имя будет отображаться рядом с работами и комментариями.</p><form onSubmit={submit} className="mt-7 space-y-4"><label className="block text-sm font-medium">Ваше имя<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required maxLength={32} className="mt-2 h-12 w-full rounded-xl border line bg-transparent px-4" placeholder="Например, Анна"/></label><label className="block text-sm font-medium">Email<input value={form.email} onChange={e=>setForm({...form,email:e.target.value})} type="email" required className="mt-2 h-12 w-full rounded-xl border line bg-transparent px-4" placeholder="you@example.com"/></label><label className="block text-sm font-medium">Пароль<input value={form.password} onChange={e=>setForm({...form,password:e.target.value})} type="password" required minLength={6} className="mt-2 h-12 w-full rounded-xl border line bg-transparent px-4" placeholder="Минимум 6 символов"/></label><button disabled={loading} className="flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-acid font-semibold text-ink disabled:opacity-60">{loading?"Создаём…":"Зарегистрироваться"}<ArrowRight size={17}/></button></form><p className="muted mt-6 text-center text-sm">Уже есть аккаунт? <Link className="font-semibold text-[var(--text)]" href="/login">Войти</Link></p></div></main>}