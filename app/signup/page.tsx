"use client";import Link from"next/link";import{useState}from"react";import{useRouter}from"next/navigation";import{BookOpen,ArrowRight}from"lucide-react";import{toast}from"sonner";import{createClient}from"@/lib/supabase/client";import{saveLocalUser}from"@/lib/local-auth";export default function Signup(){const router=useRouter(),supabase=createClient();const[loading,setLoading]=useState(false),[form,setForm]=useState({username:"",email:"",password:""});async function submit(e:React.FormEvent){e.preventDefault();if(form.username.trim().length<2)return toast.error("Имя должно содержать минимум 2 символа");if(form.password.length<6)return toast.error("Пароль должен содержать минимум 6 символов");setLoading(true);if(!supabase){saveLocalUser({username:form.username.trim(),email:form.email,authenticated:true});toast.success("Аккаунт создан на этом устройстве");router.push("/account");return}const{data,error}=await supabase.auth.signUp({email:form.email,password:form.password,options:{data:{username:form.username.trim()},emailRedirectTo:`${location.origin}/auth/callback`}});setLoading(false);if(error)return toast.error(error.message);if(data.session){toast.success("Аккаунт создан");router.push("/account")}else toast.success("Проверьте почту")};return <main className="grid min-h-screen place-items-center p-4"><div className="card w-full max-w-md rounded-3xl p-8 shadow-soft"><Link href="/" className="flex items-center gap-2 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-acid text-ink"><BookOpen size={18}/></span>coverly</Link><h1 className="mt-9 text-3xl font-bold">Создать аккаунт</h1><p className="muted mt-2">Введите имя, которое увидят другие пользователи.</p><form onSubmit={submit} className="mt-7 space-y-4"><input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required maxLength={32} className="h-12 w-full rounded-xl border line bg-transparent px-4" placeholder="Ваше имя"/><input value={form.email} onChange={e=>setForm({...form,email:e.target.value})} type="email" required className="h-12 w-full rounded-xl border line bg-transparent px-4" placeholder="Email"/><input value={form.password} onChange={e=>setForm({...form,password:e.target.value})} type="password" required minLength={6} className="h-12 w-full rounded-xl border line bg-transparent px-4" placeholder="Пароль — минимум 6 символов"/><button disabled={loading} className="flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-acid font-semibold text-ink">Зарегистрироваться<ArrowRight size={17}/></button></form><p className="muted mt-6 text-center text-sm">Уже есть аккаунт? <Link className="font-semibold" href="/login">Войти</Link></p></div></main>}