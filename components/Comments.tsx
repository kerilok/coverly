"use client";import{useState}from"react";import{AnimatePresence,motion}from"framer-motion";import{Send,Star,Trash2}from"lucide-react";import{comments as initial}from"@/lib/mock";import{useI18n}from"./I18nProvider";export function Comments(){const{t}=useI18n();const[items,setItems]=useState(initial),[text,setText]=useState("");return <section className="mt-10"><h2 className="text-2xl font-bold">{t("comments")} <span className="muted">{items.length}</span></h2><form onSubmit={e=>{e.preventDefault();if(!text.trim())return;setItems([{id:Date.now()+"",user:"You",avatar:"YO",text,date:"now",rating:9},...items]);setText("")}} className="card mt-5 flex gap-3 rounded-2xl p-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-plum text-xs font-bold text-white">YO</span><textarea value={text} onChange={e=>setText(e.target.value)} className="min-h-20 flex-1 resize-none bg-transparent p-2 outline-none" placeholder={t("commentPlaceholder")}/><button aria-label="Send" className="grid h-11 w-11 self-end place-items-center rounded-xl bg-acid text-ink"><Send size={17}/></button></form><div className="mt-4 space-y-3"><AnimatePresence>{items.map(c=><motion.article key={c.id} initial={{opacity:0,y:-10}} animate={{opacity:1,y:0}} className="card rounded-2xl p-5"><div className="flex gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-[var(--soft)] text-xs font-bold">{c.avatar}</span><div className="flex-1"><div className="flex items-center gap-2"><b className="text-sm">{c.user}</b>{c.rating&&<span className="flex items-center gap-1 rounded bg-acid px-1.5 text-xs font-bold text-ink"><Star size={10}/>{c.rating}</span>}<time className="muted text-xs">{c.date}</time>{c.user==="You"&&<button className="muted ml-auto" onClick={()=>setItems(items.filter(x=>x.id!==c.id))}><Trash2 size={15}/></button>}</div><p className="mt-2 leading-7">{c.text}</p></div></div></motion.article>)}</AnimatePresence></div></section>}