"use client";import{useRef,useState}from"react";import{AnimatePresence,motion}from"framer-motion";import{ImagePlus,X,Upload}from"lucide-react";import{toast}from"sonner";import{useI18n}from"./I18nProvider";export function UploadModal({open,onClose}:{open:boolean;onClose:()=>void}){const{t}=useI18n();const[preview,setPreview]=useState<string>();const input=useRef<HTMLInputElement>(null);const pick=(f?:File)=>{if(f?.type.startsWith("image/"))setPreview(URL.createObjectURL(f))};return <AnimatePresence>{open&&<motion.div className="fixed inset-0 z-50 grid place-items-center bg-black/60 p-4 backdrop-blur-sm" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={onClose}><motion.form onSubmit={e=>{e.preventDefault();toast.success(t("publish"));onClose()}} onMouseDown={e=>e.stopPropagation()} initial={{opacity:0,y:25}} animate={{opacity:1,y:0}} className="card max-h-[92vh] w-full max-w-2xl overflow-auto rounded-3xl p-6"><div className="mb-6 flex justify-between"><div><p className="muted text-xs uppercase tracking-widest">{t("newWork")}</p><h2 className="mt-2 text-2xl font-bold">{t("addCover")}</h2></div><button type="button" onClick={onClose}><X/></button></div><div className="grid gap-6 md:grid-cols-[220px_1fr]"><button type="button" onClick={()=>input.current?.click()} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();pick(e.dataTransfer.files[0])}} className="aspect-[3/4] overflow-hidden rounded-2xl border-2 border-dashed line">{preview?<img src={preview} alt="Preview" className="h-full w-full object-cover"/>:<span className="muted flex h-full flex-col items-center justify-center gap-3 p-4"><ImagePlus/><b>{t("drop")}</b></span>}<input ref={input} type="file" accept="image/*" hidden onChange={e=>pick(e.target.files?.[0])}/></button><div className="space-y-4"><input required className="h-12 w-full rounded-xl border line bg-transparent px-4" placeholder={t("title")}/><input required className="h-12 w-full rounded-xl border line bg-transparent px-4" placeholder={t("author")}/><textarea className="min-h-28 w-full rounded-xl border line bg-transparent p-4" placeholder={t("description")}/><button className="flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-acid font-semibold text-ink"><Upload size={17}/>{t("publish")}</button></div></div></motion.form></motion.div>}</AnimatePresence>}