"use client";import Link from"next/link";import{useState}from"react";import{useRouter}from"next/navigation";import{BookOpen,ArrowRight}from"lucide-react";import{toast}from"sonner";import{createClient}from"@/lib/supabase/client";import{getLocalUser,saveLocalUser}from"@/lib/local-auth";export default function Login(){const router=useRouter(),supabase=createClient();const[loading,setLoading]=useState(false),[email,setEmail]=useState(""),[password,setPassword]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);if(!supabase){const user=getLocalUser();if(!user||user.email.toLowerCase()!==email.toLowerCase()){setLoading(false);return toast.error("Сначала создайте аккаунт")};saveLocalUser({...user,authenticated:true});toast.success("Вы вошли");router.push("/");return}const{error}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(error)return toast.error("Неверный email или пароль");toast.success("Вы вошли");router.push("/")};return <main className="grid min-h-screen place-items-center p-4"><div className="card w-full max-w-md rounded-3xl p-8 shadow-soft"><Link href="/" className="flex items-center gap-2 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-acid text-ink"><BookOpen size={18}/></span>coverly</Link><h1 className="mt-9 text-3xl font-bold">С возвращением</h1><form onSubmit={submit} className="mt-7 space-y-4"><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required className="h-12 w-full rounded-xl border line bg-transparent px-4" placeholder="Email"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required className="h-12 w-full rounded-xl border line bg-transparent px-4" placeholder="Пароль"/><button disabled={loading} className="flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-acid font-semibold text-ink">Войти<ArrowRight size={17}/></button></form><p className="muted mt-6 text-center text-sm">Нет аккаунта? <Link className="font-semibold" href="/signup">Зарегистрироваться</Link></p></div></main>}