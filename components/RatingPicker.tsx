"use client";import{useState}from"react";import{motion}from"framer-motion";import{toast}from"sonner";export function RatingPicker(){const[value,setValue]=useState(0),[hover,setHover]=useState(0);return <div><div className="grid grid-cols-5 gap-2 sm:grid-cols-10">{Array.from({length:10},(_,i)=>i+1).map(n=><motion.button key={n} whileTap={{scale:.88}} onMouseEnter={()=>setHover(n)} onMouseLeave={()=>setHover(0)} onClick={()=>{setValue(n);toast.success(`Ваша оценка — ${n}`)}} className={`aspect-square rounded-xl border text-sm font-bold transition ${n<=(hover||value)?"border-acid bg-acid text-ink":"line"}`}>{n}</motion.button>)}</div><p className="muted mt-3 text-sm">{value?`Вы поставили ${value} из 10`:"Выберите оценку от 1 до 10"}</p></div>}