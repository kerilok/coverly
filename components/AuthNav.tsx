"use client";
import Link from"next/link";import{useEffect,useRef,useState}from"react";import{ChevronDown,LogIn,LogOut,UserCircle}from"lucide-react";import{createClient}from"@/lib/supabase/client";import{getLocalUser,clearLocalUser,type LocalUser}from"@/lib/local-auth";import{useI18n}from"./I18nProvider";import type{User}from"@supabase/supabase-js";
export function AuthNav(){const{t}=useI18n(),supabase=createClient();const[user,setUser]=useState<User|null>(null),[local,setLocal]=useState<LocalUser|null>(null),[checked,setChecked]=useState(false),[open,setOpen]=useState(false);const menu=useRef<HTMLDivElement>(null);useEffect(()=>{const sync=()=>setLocal(getLocalUser()),close=(e:MouseEvent)=>{if(!menu.current?.contains(e.target as Node))setOpen(false)};sync();window.addEventListener("coverly-auth",sync);window.addEventListener("storage",sync);document.addEventListener("mousedown",close);if(!supabase){setChecked(true);return()=>{window.removeEventListener("coverly-auth",sync);window.removeEventListener("storage",sync);document.removeEventListener("mousedown",close)}}supabase.auth.getSession().then(({data})=>{setUser(data.session?.user??null);setChecked(true)});const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,session)=>{setUser(session?.user??null);setChecked(true)});return()=>{subscription.unsubscribe();window.removeEventListener("coverly-auth",sync);window.removeEventListener("storage",sync);document.removeEventListener("mousedown",close)}},[supabase]);if(!checked)return <div className="h-11 w-11 animate-pulse rounded-full bg-[var(--soft)]"/>;const active=user||local;if(!active)return <Link href="/login" className="flex h-11 items-center gap-2 rounded-xl border line px-3 text-sm font-semibold"><LogIn size={16}/><span className="hidden sm:inline">{t("login")}</span></Link>;const name=user?.user_metadata?.username||local?.username||user?.email||"Я",avatar=user?.user_metadata?.avatar as string|undefined,color=(user?.user_metadata?.username_color as string)||"#ffffff";const logout=async()=>{if(supabase)await supabase.auth.signOut();clearLocalUser();setOpen(false);location.href="/"};return <div ref={menu} className="relative"><button onClick={()=>setOpen(v=>!v)} aria-label="Account menu" aria-expanded={open} className="flex h-11 items-center gap-1 rounded-full pr-1 focus:outline-none focus:ring-2 focus:ring-acid"><span className="grid h-11 w-11 place-items-center overflow-hidden rounded-full bg-gradient-to-br from-plum to-pink-400 text-xs font-bold text-white">{avatar?<img src={avatar} alt={name} className="h-full w-full object-cover"/>:name.slice(0,2).toUpperCase()}</span><ChevronDown size={14} className={`hidden transition sm:block ${open?"rotate-180":""}`}/></button>{open&&<div className="absolute right-0 top-full z-50 mt-2 w-56 overflow-hidden rounded-2xl border border-white/15 bg-neutral-950 p-2 text-white shadow-2xl"><div className="flex items-center gap-3 border-b border-white/10 px-3 py-3"><span className="grid h-10 w-10 shrink-0 place-items-center overflow-hidden rounded-full bg-gradient-to-br from-plum to-pink-400 text-xs font-bold">{avatar?<img src={avatar} alt="" className="h-full w-full object-cover"/>:name.slice(0,2).toUpperCase()}</span><span className="min-w-0 truncate text-sm font-bold" style={{color}}>{name}</span></div><Link href="/account" onClick={()=>setOpen(false)} className="mt-1 flex min-h-11 items-center gap-3 rounded-xl px-3 text-sm hover:bg-white/10"><UserCircle size={18}/>{t("account")}</Link><button onClick={logout} className="flex min-h-11 w-full items-center gap-3 rounded-xl px-3 text-left text-sm text-red-300 hover:bg-red-500/10"><LogOut size={18}/>{t("logout")}</button></div>}</div>}