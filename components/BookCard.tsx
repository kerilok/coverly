"use client";import Link from"next/link";import{motion}from"framer-motion";import{MessageCircle,Star}from"lucide-react";import type{Book}from"@/lib/types";import{BookCover}from"./BookCover";export function BookCard({book,index}:{book:Book;index:number}){return <motion.article initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{delay:index*.05}} whileHover={{y:-6}}><Link href={`/books/${book.id}`} className="block rounded-2xl"><div className="card overflow-hidden rounded-2xl p-2 shadow-sm hover:shadow-soft"><BookCover text={book.cover} palette={book.palette} className="aspect-[3/4] rounded-xl"/><div className="px-2 pb-2 pt-4"><div className="flex justify-between gap-3"><div><h3 className="font-semibold">{book.title}</h3><p className="muted mt-1 text-sm">{book.author}</p></div><div className="flex h-fit items-center gap-1 rounded-lg bg-acid px-2 py-1 text-sm font-bold text-ink"><Star size={13} fill="currentColor"/>{book.rating}</div></div><div className="muted mt-4 flex gap-4 text-xs"><span>{book.ratings} оценок</span><span className="flex items-center gap-1"><MessageCircle size={13}/>{book.comments}</span></div></div></div></Link></motion.article>}